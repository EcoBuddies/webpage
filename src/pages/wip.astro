---
import Layout from '~/layouts/Layout.astro';
import { getHomePermalink } from '~/utils/permalinks';

const title = `Stran v izdelavi`;
---

<Layout metadata={{ title }}>
  <section class="flex items-center h-full p-16">
    <div class="container flex flex-col items-center justify-center px-5 mx-auto my-8">
      <div class="max-w-md text-center">
        <h2 class="mb-8 font-bold text-6xl">
          <span class="text-primary">Stran v izdelavi</span>
        </h2>
        <p class="mt-4 mb-8 text-lg text-muted dark:text-slate-400">
          Sledi nam na družbenih omrežjih, kjer te bomo obvestili, ko bo stran na voljo!
        </p>
        <a rel="noopener noreferrer" href={getHomePermalink()} class="btn ml-4">Nazaj domov</a>
      </div>
    </div>
  </section>
</Layout>
